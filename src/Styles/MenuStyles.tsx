import {makeStyles} from "@material-ui/core/styles";

const useStyles = makeStyles({
    root: {
        width: '100%',
        position: 'fixed',
        left: '0',
        bottom: '0',
    },
});

export default useStyles